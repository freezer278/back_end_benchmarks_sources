
export enum LOG_TRANSPORT {
  CONSOLE_JSON = 'console_json',
  CONSOLE = 'console',
  STACK = 'stack',
}

export enum LOG_LEVEL {
  ERROR = 'error',
  WARN = 'warn',
  INFO = 'info',
  DEBUG = 'debug',
}

export enum ENV {
  LOCAL = 'local',
  DEV = 'dev',
  PROD = 'prod',
}
